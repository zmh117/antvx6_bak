## 1. 模型与现状核对

- [x] 1.1 核对 `processContainerJson`、`containerNodeKey`、BPMN Profile、节点坐标和组件模板 DTO 的当前读写路径，列出仍缺失的透传点。
- [x] 1.2 确认流程容器判定函数只把 `SUB_PROCESS/EMBEDDED` 且带容器配置的节点作为可嵌入容器，普通事务子流程不自动获得拖入能力。
- [x] 1.3 确认流程容器不能拖入另一个流程容器，并为该规则补齐前端与后端校验。

## 2. X6 容器交互

- [x] 2.1 调整流程容器节点渲染样式，明确标题区、内容区、最小尺寸和内部节点安全边距。
- [x] 2.2 实现拖拽过程中的流程容器命中检测和高亮反馈，命中范围以容器内容区为准。
- [x] 2.3 实现节点拖入流程容器时的批处理提交：切换 X6 parent、转换相对坐标、写入 `containerNodeKey`、刷新边和触发保存状态。
- [x] 2.4 实现节点拖出流程容器时的批处理提交：挂回泳道、转换相对泳道坐标、清空 `containerNodeKey`、刷新边和触发保存状态。
- [x] 2.5 实现节点从一个流程容器拖到另一个流程容器时的重归属逻辑。
- [x] 2.6 调整容器自动 fit 和泳道自动 fit 顺序，确保内部节点不被标题区遮挡，容器扩展后泳道也能扩展。
- [x] 2.7 确认移动流程容器时内部节点与相关连线视觉上一起移动，且内部节点相对坐标不被错误改写。
- [x] 2.8 保留非空流程容器删除保护，必要时补充 UI 提示或禁用态。
- [x] 2.9 支持流程容器缩放，并限制最小尺寸不遮挡内部节点。

## 3. 业务流程保存与协作

- [x] 3.1 更新业务图增量 ops 构建逻辑，确保拖入、拖出和跨容器移动会产生 `containerNodeKey` 与坐标 patch。
- [x] 3.2 更新业务图从后端记录重建 X6 画布的顺序，确保先创建容器再挂载内部节点。
- [x] 3.3 更新 Yjs 协作文档写入和读取，确保 `container_node_key` 在协作同步和 materialize 后保持一致。
- [x] 3.4 更新历史快照与恢复路径，确保流程容器、内部节点、相对坐标和连线恢复一致。
- [ ] 3.5 验证跨容器边界连线保存后再次打开仍连接到正确端口。

## 4. 泳道组件模板

- [x] 4.1 在泳道组件编辑器中复用业务图流程容器拖入、拖出、命中反馈和 fit 逻辑。
- [x] 4.2 更新组件草稿生成逻辑，确保内部节点保存容器相对坐标和 `containerNodeKey`。
- [x] 4.3 更新组件发布和放置到业务图实例逻辑，确保节点 key 重映射时同步重写 `containerNodeKey`。
- [ ] 4.4 验证包含流程容器的组件模板放置后仍保留内部节点和跨边界连线。

## 5. 后端校验与 Agent Context

- [x] 5.1 补齐后端流程容器层级校验：容器必须存在、不能引用自身、不能形成容器嵌套或环。
- [x] 5.2 补齐流程容器质量问题：空容器、`reusableCall` 缺少引用、内部节点丢失父容器。
- [x] 5.3 确认 Agent context 输出 containers、内部节点 `containerNodeKey`、所属泳道和 BPMN 类型。
- [x] 5.4 确认 Agent context 根据 source/target 容器关系输出 `insideContainer`、`crossContainerBoundary` 和 `topLevel`。

## 6. 验证

- [x] 6.1 运行 `npm run build --prefix front`。
- [x] 6.2 运行后端 Python 编译或项目现有后端测试。
- [x] 6.3 运行 `openspec validate make-process-container-embedded-drop-zone --strict`。
- [ ] 6.4 使用浏览器验证业务流程图：拖入容器、拖出容器、容器间移动、移动容器、内部连线、跨边界连线。
- [ ] 6.5 使用浏览器验证泳道组件编辑器和组件放置：模板内拖入拖出、发布、放置、重新打开。
