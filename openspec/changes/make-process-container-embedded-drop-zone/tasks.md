## 1. 前端入口移除

- [x] 1.1 从 BPMN 工具箱删除“流程容器”节点。
- [x] 1.2 删除业务图属性面板中的“流程容器”配置区。
- [x] 1.3 删除泳道组件属性面板中的“流程容器”配置区。
- [x] 1.4 确认 Task UI Steps/Page 字段保留。

## 2. X6 交互移除

- [x] 2.1 移除流程容器命中、高亮、拖入、拖出和重归属逻辑。
- [x] 2.2 移除流程容器缩放能力。
- [x] 2.3 移除流程容器非空删除保护。
- [x] 2.4 移除流程容器自动 fit 与内部布局逻辑。
- [x] 2.5 确认泳道移动、泳道缩放和普通节点连线仍保留。

## 3. 旧数据兼容

- [x] 3.1 打开旧业务图时过滤旧流程容器节点。
- [x] 3.2 旧内部节点清空 `containerNodeKey` 并恢复为泳道顶层节点。
- [x] 3.3 过滤连接到旧流程容器节点的连线。
- [x] 3.4 协作全量和增量路径清洗旧流程容器数据。
- [x] 3.5 保留数据库字段，不做破坏性迁移。

## 4. 后端与 Agent Context

- [x] 4.1 后端 `process_container_json` 语义规整为空对象。
- [x] 4.2 Agent Context 过滤旧流程容器节点。
- [x] 4.3 Agent Context 不再输出流程容器列表和容器边作用域。
- [x] 4.4 历史字段残留不阻断旧数据读取。

## 5. 验证

- [x] 5.1 运行 `npm run build --prefix front`。
- [x] 5.2 运行 `python3 -m compileall back/app`。
- [x] 5.3 运行 `openspec validate make-process-container-embedded-drop-zone --strict`。
- [ ] 5.4 浏览器验证业务图和泳道组件工具箱、属性面板、旧图打开效果。
